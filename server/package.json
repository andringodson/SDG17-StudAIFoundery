{
  "name": "sdg17-realtime-server",
  "version": "1.0.0",
  "private": true,
  "description": "Telegram bot auth + WebSocket live-poll broadcaster for the SDG 17 platform. Runs as a persistent Node process — deploy to Railway/Render/Fly, not Vercel (serverless can't hold a WebSocket or a long-polling bot open).",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "telegraf": "^4.16.3",
    "ws": "^8.18.0",
    "pg": "^8.13.1",
    "express": "^4.21.2",
    "dotenv": "^16.4.7"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
